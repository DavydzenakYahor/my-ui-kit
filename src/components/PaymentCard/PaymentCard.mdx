import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as PaymentCardStories from './PaymentCard.stories';

<Meta of={PaymentCardStories} />

# PaymentCard

Displays a saved payment card with cardholder details on the front and CVV on the back. Click or press Enter/Space to flip.

## Visa

<Canvas of={PaymentCardStories.Visa} />

## Mastercard

<Canvas of={PaymentCardStories.Mastercard} />

## Amex

<Canvas of={PaymentCardStories.Amex} />

## Empty

Preview state before the user fills in their card details — pass masked placeholders for each field.

<Canvas of={PaymentCardStories.Empty} />

## Skeleton

Use while card data is being fetched. Flip is disabled.

<Canvas of={PaymentCardStories.Skeleton} />

## Props

<Controls />
